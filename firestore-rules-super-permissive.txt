// REGRAS SUPER PERMISSIVAS - APENAS PARA TESTE E CORREÇÃO
// ⚠️ ATENÇÃO: Estas regras são temporárias para resolver o problema
// Projeto: klientti-640d4

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PERMITIR TUDO TEMPORARIAMENTE
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
